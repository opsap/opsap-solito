{
  "pipeline": {
    "build": {
      "dependsOn": ["^build"],
      "outputs": [
        ".next/**",
        "generated-wundergraph/**",
        "generated/**",
        ".vercel/**"
      ]
    }
  },
  "globalEnv": ["NODE_ENV", "WG_ALLOWED_ORIGIN"]
}
